---
import { Sparkles, Hand, Target, BookOpen, HandHelping, Bot, FlaskConical, Rocket, User, Mail } from 'lucide-astro';
import Layout from '../layouts/Layout.astro';
---

<Layout title="关于" description="关于摸索 AI - 记录 AI Agent 开发的探索之路">
  <article class="about">
    <header class="header">
      <Sparkles size={64} class="header-icon" />
      <h1>关于摸索 AI</h1>
    </header>

    <section class="section">
      <Hand size={32} class="section-icon" />
      <h2>这里是哪里？</h2>
      <p>
        摸索 AI 是一个记录 AI Agent 开发探索之路的地方。这里分享 OpenClaw、Hermes Agent
        等工具的实战经验、教程和心得。
      </p>
    </section>

    <section class="section">
      <Target size={32} class="section-icon" />
      <h2>为什么要做这个？</h2>
      <p>
        AI Agent 是个新领域，文档少、坑多。我在摸索过程中踩了不少坑，也积累了一些经验。
        把这些记录下来，希望能帮到同样在探索的人。
      </p>
    </section>

    <section class="section">
      <BookOpen size={32} class="section-icon" />
      <h2>内容方向</h2>
      <div class="grid">
        <div class="card">
          <HandHelping size={32} class="card-icon" />
          <h3>OpenClaw 教程</h3>
          <p>安装、配置、Skill 开发、部署</p>
        </div>
        <div class="card">
          <Bot size={32} class="card-icon" />
          <h3>Hermes Agent</h3>
          <p>使用指南、实战案例</p>
        </div>
        <div class="card">
          <FlaskConical size={32} class="card-icon" />
          <h3>AI 工具评测</h3>
          <p>各种 AI 开发工具的对比和推荐</p>
        </div>
        <div class="card">
          <Rocket size={32} class="card-icon" />
          <h3>项目实战</h3>
          <p>从零到一构建 AI Agent 项目</p>
        </div>
      </div>
    </section>

    <section class="section">
      <User size={32} class="section-icon" />
      <h2>关于作者</h2>
      <p>
        老李，一个在 AI Agent 领域摸索的开发者。有糖尿病，总是熬夜，做事有点拖拉，
        但认真起来会忘记时间。
      </p>
    </section>

    <section class="section">
      <Mail size={32} class="section-icon" />
      <h2>联系我</h2>
      <p>
        有问题或建议？欢迎交流。
      </p>
    </section>
  </article>
</Layout>

<style>
  .about {
    max-width: 800px;
    margin: 0 auto;
  }

  .header {
    text-align: center;
    padding: 3rem 0;
    margin-bottom: 3rem;
  }

  .header-icon {
    color: var(--primary-light);
    margin-bottom: 1rem;
    animation: float 3s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  .header h1 {
    font-size: 2.5rem;
    font-weight: 800;
    background: linear-gradient(135deg, var(--primary-light) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section {
    margin-bottom: 2.5rem;
    padding: 2rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    position: relative;
    overflow: hidden;
  }

  .section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--primary) 0%, var(--accent) 100%);
    opacity: 0.5;
  }

  .section-icon {
    color: var(--primary-light);
    margin-bottom: 1rem;
  }

  .section h2 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text);
  }

  .section p {
    color: var(--text-muted);
    line-height: 1.8;
    font-size: 1rem;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .card {
    background: var(--bg-card-hover);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    transition: all 0.3s ease;
  }

  .card:hover {
    border-color: var(--primary);
    transform: translateY(-4px);
    box-shadow: var(--shadow-md), 0 0 20px rgba(124, 58, 237, 0.15);
  }

  .card-icon {
    color: var(--primary-light);
    margin-bottom: 0.75rem;
  }

  .card h3 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text);
  }

  .card p {
    color: var(--text-dim);
    font-size: 0.9rem;
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .header h1 {
      font-size: 2rem;
    }

    .section {
      padding: 1.5rem;
    }

    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
